{{#compress}}
<!DOCTYPE html>
<html lang="{{ __.Site.language }}">
    <head>
        {{> site/metas }}
        <title>{{ config.site.title }}</title>
        <link rel="stylesheet" href="/css/style.css">
    </head>
<body>
    {{> site/header }}

    <div class="container">
        {{{ body }}}
    </div>

    {{> development/livereload }}
</body>
</html>
{{/compress}}